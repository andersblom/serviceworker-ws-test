# Testing WebSocket's with ServiceWorkers

## Setup
Prerequisites: Node 14 or greater to support `import` statements in the Node server source.
1. Run `npm install`
2. Run `npm run dev`
3. Visit localhost:8080 in your browser.